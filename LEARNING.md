# AI Factory 售前架构师 · 学习手册 v2

> 配套工具：本仓库的模拟器（线上 https://caoruixin.github.io/aifactory-sim/ ，本地 `npm run dev`）。
> 下文所有「打开 `?...`」都指把查询串拼在应用地址后面，例如
> `https://caoruixin.github.io/aifactory-sim/?level=rack&focus=asm.gb300.rack`。
> v2 在 v1 四周计划的基础上补齐了**效果度量体系**：学前基线测试、每周量化验收线、
> 终验评分表、进度追踪表——每一步都能回答「我到底学没学会」。
> 每周投入约 5 小时：官方资料 1.5h · 模拟器练习 2h · 脱稿讲解 1h · 产出物 0.5h。

## 唯一主线（所有术语都挂在这条线上）

```
业务负载 → 模型/SLA → 推理软件 → 并行与副本 → Scale-Up/Scale-Out
        → 服务器/托盘/机架 → 供电液冷 → 产能/TCO → POC
```

遇到任何新术语，只回答三个问题：**它在主线哪一段？做什么？影响哪个指标？**
不确定就在模拟器里点开它，看详情面板的「售前怎么解释」卡。

---

## 0. 效果如何衡量（先读这节）

三级度量，全部记进文末「进度追踪表」：

| 度量 | 什么时候 | 及格线 | 衡量什么 |
|---|---|---|---|
| **基线测试**（20 题） | 开始学习前 30 分钟 | 无及格线，只记分 | 起点水位 |
| **周验收**（每周末） | 每周第 5 小时 | 各周验收线见正文，**没过就不进下一周** | 过程质量 |
| **终验**（第 4 周末） | 15 分钟脱稿汇报 + 重做基线测试 | 汇报 ≥80/100 且追问 ≥6/10；基线重测 ≥18/20 | 最终效果 |

**学习效果 = 基线重测分 − 基线初测分**（典型起点 6~10 分，目标终点 ≥18 分）。
所有自测题的答案与核对位置在附录 A——先作答、再核对，核对不过的当场在模拟器里点开确认。

## 1. 基线测试（学前 30 分钟，闭卷）

不查资料作答，每题 1 分，答案见附录 A。做完把得分记入进度追踪表，然后再开始第 1 周。

**结构（对应第 1 周）**
1. 一台 GB300 NVL72 机架里有多少个计算托盘、多少个 NVLink 交换托盘？
2. 每个计算托盘里有什么（CPU/GPU/网卡/DPU 各几个）？
3. 「NVL72」的 72 指什么？怎么算出来的？
4. 机房里除了机架列，还有哪 5 类设备？
5. 为什么说 AI Factory 的「产品」是 token？

**物理系统（对应第 2 周）**
6. 六个网络与设施平面分别是什么？
7. NVLink 和 Scale-Out 为什么必须是两张网？
8. 每颗 GPU 的 NVLink 带宽是多少？
9. 机房供电链路从市电到 GPU 依次经过哪些环节？
10. CDU 是干什么的？一次侧/二次侧分别指什么？

**推理与瓶颈（对应第 3 周）**
11. 推理数据流 10 步里，哪些步骤不产生机架内物理流量？
12. Prefill 和 Decode 分别主要消耗什么资源？
13. KV Cache 存在哪里？「KV 写入」这一步涉及哪三个部件？
14. MoE 为什么把瓶颈从计算转向通信？这些流量走哪张网？
15. 为什么不能拿卡数 × 峰值 FLOPS 直接算产能？
16. TTFT 和 TPOT 分别主要由哪个阶段决定？

**代际与证据（对应第 4 周）**
17. GB300 → Vera Rubin 最大的三个代际变化是什么？各自的数字是多少？
18. 售前对外要区分的三大类证据是什么？工具内部一共分几档？
19. 为什么 Vera Rubin 的 tokens/W 不出数？
20. Rubin Ultra NVL576 已经官宣了，为什么工具仍然拒绝给它出产能？

---

## 2. 第 1 周 · 建立总图与前 15 张术语卡

**本周目标**：能画出 AI Factory 总图；15 个核心术语每个能一句话说清「在哪里、做什么、影响什么」。

**环节 1.1 走完导览（40 分钟）**

> GB300 共 **10 站 = 3 个讲解站 + 7 个练习站**（v1.3 起）。
> 讲解站是主线（机房 → 机架 → 托盘），练习站是六平面各一站 + 交换层对照一站，
> 分别安排在环节 2.1 与第 4 周。**本环节只走前 3 个讲解站。**
> 每个站都能用 `?tour=<场景 id>` 直达；打开后左栏会自动滚到那一站并展开三段式讲解，
> 3D 里这一站点名的部件会被高亮（青色）。

1. 打开应用 → 左栏「场景导览」走完第 1~3 站。
2. 每站结束后合上页面，口述这一站讲了什么，再打开对照。

**环节 1.2 集群总览认设备（40 分钟）· 任务卡**

- **▶ 打开**：<https://caoruixin.github.io/aifactory-sim/?tour=scene.gb300.cluster-overview>
- **预期看到**：一列 8 台机架 + CDU + Spine 主干被高亮成青色（这一站点名的三件），机房供电与冷却水干线同时可见。
- **操作（3 步）**：
  1. 左栏平面开关全部勾上；
  2. 顺着橙色线走一遍「机房配电柜 → 8 台机架」的供电扇出，再顺着青色线走一遍「歧管 → CDU → 一次侧水路」；
  3. 逐个点开并读右栏「售前怎么解释」：机架列、Scale-Out 主干/叶交换、汇聚交换层、CDU、外置存储、管理节点、**机房配电柜**（机架列端头的橙色柜）。
- **自问三问**：它在主线哪一段？做什么？影响哪个指标？
- **核对位置**：右栏部件详情 + 附录 A 第 4 题。
- 顺手确认一件事：随意拖拽旋转细看，视角不会被程序抢走（v1.1 起点步骤/调窗口都不再复位相机）。

**环节 1.3 下钻一遍（40 分钟）**
集群 → `?level=rack&focus=asm.gb300.rack` → 双击托盘 → 板级，每层停下来读右栏详情与「售前怎么解释」。

**环节 1.4 做前 15 张术语卡（60 分钟）**
卡片模板见第 7 节。本周 15 张：GPU(B300) / Grace CPU / HBM / NVLink / NVSwitch /
Compute Tray / NVSwitch Tray / ConnectX(NIC) / BlueField(DPU) / Spectrum-X(Scale-Out) /
Power Shelf / Busbar / CDU / 冷板 / Rack U。素材都在对应部件的详情面板里。

**环节 1.5 脱稿讲解（60 分钟：准备 30 + 讲 2×3 分钟 + 复盘）**
对着 `?level=rack` 画面讲「一台 GB300 NVL72 机架里都有什么」，录音。

**✅ 第 1 周验收线（全部达标才进第 2 周）**
- [ ] 手画总图含 ≥8 个要素（机架列/两张网/存储/管理/配电/CDU/水路/托盘结构），对着集群视图逐一指认无误
- [ ] 15 张术语卡齐，随机抽 5 张，每张 ≤20 秒说出「位置/作用/影响指标」三要素，≥4 张过
- [ ] 3 分钟机架讲解录音：不看稿、卡壳 ≤2 次、含「18+9」「2+4」两组数字且准确
- [ ] 周测（基线测试第 1~5 题重做）≥4/5

## 3. 第 2 周 · 看懂物理系统（六平面）

**本周目标**：5 分钟内从 GPU 讲到 NVSwitch、ConnectX、BlueField、Spectrum-X、Power Shelf、CDU 不卡壳。

**环节 2.1 六平面逐个开关（60 分钟）· 六张任务卡 + 一张对照卡**

每张卡对应模拟器里的一个**练习站**：打开链接就落到「机架级 + 只开那一个平面」，
左栏给出三段式讲解（① 你应该看到什么 / ② 谁连谁 + 关键数字 / ③ 断了会怎样），
3D 里这一站点名的部件会高亮。**先自己答三问，再读左栏对答案。**

**卡 1 · NVLink（机架内 scale-up）**
- ▶ 打开：<https://caoruixin.github.io/aifactory-sim/?tour=scene.gb300.learn-plane-nvlink>
- 预期看到：整架只剩机架内的绿色连线，计算托盘与交换托盘被高亮，没有一条线离开机架。
- 操作：① 先不看左栏，自己答三问；② 双击一个计算托盘下钻到板级，找到 GPU ↔ NVSwitch 那条边；③ 回到机架级读左栏。
- 自问三问：谁连谁？关键数字多少？断了会怎样？
- 核对位置：`asm.gb300.b300-gpu` 详情（每卡 1.8 TB/s）+ 机架详情（130 TB/s / 18 颗 NVSwitch）。

**卡 2 · Scale-Out（机架之间的计算网）**
- ▶ 打开：<https://caoruixin.github.io/aifactory-sim/?tour=scene.gb300.learn-plane-scaleout>
- 预期看到：线从托盘里的网卡出发穿出机架，去找机架外的 Leaf——与卡 1 正好相反。
- 操作：① 答三问；② 点机架边界上通往 Leaf 的「→ …」传送门标签，看它选中了哪台设备；③ 对比卡 1 说出「两张网各解决什么」。
- 自问三问：谁连谁？关键数字多少？断了会怎样？
- 核对位置：`asm.gb300.cx8-nic` 详情（800 Gb/s、1:1 GPU:NIC、拆成 2×400 Gb/s 接两台 leaf）。

**卡 3 · 业务与存储（南北向）**
- ▶ 打开：<https://caoruixin.github.io/aifactory-sim/?tour=scene.gb300.learn-plane-business>
- 预期看到：BlueField-3 → 汇聚交换层 → 外部存储这条链路，与计算网完全不重叠。
- 操作：① 答三问；② 点开 BlueField-3 与汇聚交换层；③ 用一句话说清「这张网和计算网为什么必须分开」。
- 自问三问：谁连谁？关键数字多少？断了会怎样？
- 核对位置：`asm.gb300.bf3-dpu` 详情（双 400 Gb/s）+ 连接「汇聚交换机 ↔ 外部存储」（每节点 40 GB/s 上限）。

**卡 4 · 管理（带外/带内）**
- ▶ 打开：<https://caoruixin.github.io/aifactory-sim/?tour=scene.gb300.learn-plane-mgmt>
- 预期看到：细线把托盘、交换托盘、电源架、DPU 全串到机架内管理交换机，再上联到机架外。
- 操作：① 答三问；② 点开机架内 SN2201 与带外管理汇聚；③ 找出「主机被攻破也不丢管理面」靠的是哪个部件。
- 自问三问：谁连谁？关键数字多少？断了会怎样？
- 核对位置：`asm.gb300.inrack-mgmt-switch` 与 `asm.gb300.bf3-dpu` 详情（各 1 Gb/s 带外口）。

**卡 5 · 供电**
- ▶ 打开：<https://caoruixin.github.io/aifactory-sim/?tour=scene.gb300.learn-plane-power>
- 预期看到：机房配电柜 → 8 个电源架 → 直流母排 → 每个托盘，一条自上而下的主干。
- 操作：① 答三问；② 点开电源架看 33 kW 的出处；③ 说出 142 kW 的官方口径原文是什么。
- 自问三问：谁连谁？关键数字多少？断了会怎样？
- 核对位置：机架详情的「**整机架功率**」一行（up to 142 kW；v1.5 起规格表显示中文标签，
  原字段名 `rackPowerKW` 移到了鼠标悬停提示里）+ 连接「电源架 → 直流母排」。
- ⚠️ **别把余量说成冗余**：8 × 33 kW = 264 kW 是**装机容量**，对 142 kW 的最高负载留有约
  1.9 倍余量；官方从未声明供电冗余拓扑（N+1 还是 2N）。余量 ≠ 冗余模式，说反了会被机电顾问纠正。

**卡 6 · 液冷**
- ▶ 打开：<https://caoruixin.github.io/aifactory-sim/?tour=scene.gb300.learn-plane-cooling>
- 预期看到：冷板 → 托盘冷板 → 歧管 → CDU → 机房一次侧水，一条闭环。
- 操作：① 答三问；② 点开 CDU，说清一次侧/二次侧分界在哪；③ 找出交换托盘是不是也液冷。
- 自问三问：谁连谁？关键数字多少？断了会怎样？
- 核对位置：CDU 详情 + 连接「交换托盘冷板 ↔ 分液歧管」。

**卡 7 · 三个交换层各管什么（对照卡）**
- ▶ 打开：<https://caoruixin.github.io/aifactory-sim/?tour=scene.gb300.learn-switch-layers>
- 预期看到：集群总览里 Leaf、Spine、汇聚三处同时高亮，计算网（紫）与业务网（蓝）两个平面同开。
- 操作：① 用九字框架 **leaf=接入 / spine=主干 / 汇聚=另一张网** 逐个对号；② 点开三者，确认它们是**同一款交换机分饰三层**；③ 说出三者各自断掉的后果。
- 自问三问：谁连谁？关键数字多少？断了会怎样？
- 核对位置：`cmp.gb300.sn5610` 详情的「售前怎么解释」（同一型号分饰三层）。
- ⚠️ **型号别说死**（v1.5 订正）：同一份参考架构里两种写法并存——Networking Hardware 一节写
  **SN5610**（64 口 × 800 Gb/s），Network Logical Architecture 的 Table 5 与 Appendix Table 11
  写 **SN5600**（128 口 × 400 Gb/s）。工具取 SN5610 作主值，并在 `modelNameConflict` 一行
  写明了取舍理由与「按出现次数 SN5600 更多」。对客户讲交换层时说**端口数 × 速率**（折算总容量
  相同），比说型号安全。

**补充练习（房间级干线）**：`?level=cluster&planes=power,cooling` 看配电柜→8 台机架扇出与
歧管→CDU→一次侧水；再换 `?level=cluster&planes=business,mgmt` 看 DPU→业务交换机→存储与带外上联。

**环节 2.2 关键数字过关（30 分钟）**
默写下面 8 组数字，再逐个到详情面板核对出处：
18 计算托盘 · 9 交换托盘 × 2 ASIC · 72 GPU / 36 Grace · 每 GPU NVLink 1.8 TB/s ·
机架至高 142 kW · 每托盘 2 Grace + 4 B300 + 4×CX-8 + 1×BF-3。

**环节 2.3 Scale-Up vs Scale-Out（30 分钟）**
对比 `planes=nvlink`（机架内一跳全连）与 `planes=scaleout`（rail-optimized 跨机架），
写 100 字说清：为什么是两张网、各自解决什么问题、各自的失效影响。

**环节 2.4 补齐后 15 张术语卡（60 分钟）**
管理网/带外管理 · rail-optimized · fat-tree · 歧管/一次侧二次侧水路 · E1.S/NVMe ·
电源冗余 · PUE · 超节点 · NVLink 域 · C2C · MoE · KV Cache · TTFT/TPOT · MFU/MBU ·（自选 1 张）。

**环节 2.5 脱稿讲解（60 分钟）**
5 分钟「从 GPU 到机房」物理链路讲解，对着 `?level=rack` 讲，录音自评（评分卡见第 8 节 B 表）。

**✅ 第 2 周验收线**
- [ ] 30 张术语卡集齐；全量抽检 ≥27/30 张能 ≤20 秒说清三要素
- [ ] 六平面每个平面 60 秒内答出「谁连谁/关键数字/断了会怎样」，6/6 过
- [ ] 8 组关键数字闭卷默写 ≥7 组全对
- [ ] 5 分钟讲解自评（B 表）≥12/16
- [ ] 自检题：客户问「为什么必须液冷」——用机架功率和风冷极限作答，能引用 142 kW 与 CDU 详情

## 4. 第 3 周 · 推理如何落在硬件上（售前的分水岭）

**本周目标**：能解释容量、算力、带宽、通信、尾延迟、功率六种瓶颈分别怎样限制 token 生产。

**环节 3.1 数据流精读（60 分钟）· 三张任务卡**

**卡 A · 10 步走一遍，每步点名硬件**
- ▶ 打开：<https://caoruixin.github.io/aifactory-sim/?motion=off>
- 预期看到：底部步骤条 10 步，每步卡片上带「逻辑层/物理层」徽章。
- 操作：① 先点底部 ▶ 连看 3 遍不作笔记；② 换成逐步点击，读每步文案；③ 每步用「本步涉及」chips 点名参与硬件（点 chip 会选中部件并把右栏切到详情）。
- 自问三问：这一步在主线哪一段？搬的是什么？走哪张网（或根本不走网）？
- 核对位置：第 4 步「写入 KV Cache」应出现 B300 GPU / HBM3e 显存堆栈 / Grace CPU 三个 chip；附录 A 第 11、13 题。

**卡 B · 板级看「线去哪了」**
- ▶ 打开：<https://caoruixin.github.io/aifactory-sim/?level=board&focus=asm.gb300.compute-tray&motion=off>
- 预期看到：通往托盘外的线不再横穿全屏，而是以「→ 某某」传送门标签在托盘边界收口。
- 操作：① 点第 2 步（请求接入）；② 点一个传送门标签，看它选中了哪个远端设备（相机不会飞走）；③ 说出这条线属于六平面里的哪一个。
- 自问三问：这一跳从哪来到哪去？为什么它在板级视角里只能画成一个标签？
- 核对位置：右栏部件详情标题 + 连接列表里同名连接的平面色点。

**卡 C · 机架级看请求进出**
- ▶ 打开：<https://caoruixin.github.io/aifactory-sim/?level=rack&focus=asm.gb300.rack&motion=off>
- 预期看到：第 2 / 9 步的粒子在机架边界的「→ 汇聚交换机」传送门处进出（第 2 步方向朝内）。
- 操作：① 点第 2 步看方向；② 点第 9 步看反向；③ 点第 4 步（KV 写入）——它没有连线，改为计算托盘整体发光（播放时还会呼吸）。
- 自问三问：哪些步骤有机架内流量？哪些只有「本地物理动作」？哪些纯逻辑？
- 核对位置：步骤卡的「逻辑层/物理层」徽章；第 3–8 步的板级细节留到卡 B 的视角看。

**环节 3.2 四个心智模型（30 分钟，每个都要能复述）**
① 权重常驻 HBM，不随请求加载；② Prefill 吃算力、Decode 吃显存带宽；
③ KV Cache 是显存里会长大的部分；④ MoE 把瓶颈从计算转向 Token 搬运（All-to-All 走 NVLink）。

**环节 3.3 产能粗估对照实验（60 分钟）**
右栏「产能粗估」tab，做三组对照并**填表**（每组只动一个变量）：

| 实验 | 变量 | tokens/s | TTFT | TPOT | tokens/W | 用哪个术语解释 |
|---|---|---|---|---|---|---|
| 1 | 精度 FP8→FP4 | | | | | |
| 2 | 负载 轻→重 | | | | | |
| 3 | 上下文 短→长 | | | | | |

**环节 3.4 理解「不出数」（30 分钟）**
两种拒绝要分清：
① 切 Vera Rubin 看 tokens/W 为什么单项拒绝——官方未公布整机架功率，分母缺失就那一项不出数；
② 切 `?gen=sys.rubin-ultra-nvl576` 看**整体**拒绝——这一代**已经官宣**（顶栏徽章是「已发布」不是「预测」），
但机架内部规格主要来自第三方分析师，所以产能整体不出数。
读一遍顶部警示条与产能卡的拒绝文案，练习向客户区分「官方确认的拓扑」和「分析师推测的规格」。

**环节 3.5 脱稿讲解（60 分钟）**
5 分钟对着数据流动画讲「一次推理请求的一生」，录音。

**✅ 第 3 周验收线**
- [ ] 「六种瓶颈如何限制 token 产能」一页纸完成（每种瓶颈：触发条件 + 表现指标 + 缓解手段）
- [ ] 对照实验表 3 行填满，每行的「为什么」能用第 2 周术语说通
- [ ] 「一次请求的一生」讲解：10 步顺序零错、**每步至少点名 1 个参与硬件**（用 chips 核对）、
      逻辑步/物理步区分正确
- [ ] 自检题：老板问「为什么不能拿卡数×峰值 FLOPS 算产能」——用 roofline + MFU/MBU 区间作答
- [ ] 周测（基线测试第 11~16 题重做）≥5/6

## 5. 第 4 周 · 代际叙事与 15 分钟汇报

**本周目标**：脱稿完成 15 分钟汇报，接住三个追问。

**环节 4.1 比较模式（60 分钟）**
1. `?mode=compare&right=sys.vera-rubin-nvl72`——过一遍 diff 列表，记住三个最大代际变化
   （GPU/HBM 代际、NVLink 带宽、交换芯片数量）并能说清「为什么这么改」。
2. 练习卡 · 三个代际变化各在哪里：
   ▶ 打开 <https://caoruixin.github.io/aifactory-sim/?tour=scene.rubin.learn-gen-delta> ——
   机架结构没变（还是 18 + 9），变化全在交换托盘与网卡这两处；左栏三段式讲解给出
   1.8 → 3.6 TB/s、CX-8×4 → CX-9×8、18 → 36 颗交换芯片三组数字，自己先答再对。
3. `?mode=compare&right=sys.rubin-ultra-nvl576`——练习「这一半是预测」的表达方式。

**环节 4.1b 规模阶梯与命名口径（30 分钟，第 4 周新增讲解点）**

客户最常问的不是「快多少」，而是「这些名字到底什么关系」。三件事讲清就够：

1. **封装 ≠ die**。官方口径一张 GPU = **一个封装**；Rubin 一个封装里是 2 颗 die。
   所以「Rubin Ultra NVL576 有 1152」这句话要追问一句「1152 什么」——那是**die 数**
   （576 封装 × 2，且是分析师口径），不是 GPU 卡数。混着说会让懂行的客户当场打折扣。
2. **阶梯是 NVL72 → NVL144 → NVL576，再往上是 Kyber NVL1152**。
   官方 2026-03 的 POD 博客给出 Vera Rubin Ultra 的**三档 NVLink 域**：NVL72、
   NVL144（Kyber，单机架）、旗舰 NVL576（8 个 MGX NVL 机架 × 72）；同一篇博客里
   还有「NVIDIA Kyber NVL1152」一节，指向 Kyber 线更上一档。
   **不是从 72 直接跳 576**，中间有 144 这一档。⚠️ **Kyber 的定位按官方原话说**（v1.5 订正）：
   POD 博客写的是「NVIDIA Kyber is the **next-generation** MGX NVL rack design」，2025-10 的
   OCP 博客称它是「the **successor** to NVIDIA Oberon」——即 MGX NVL 机架的**下一代**，
   不是「独立于 MGX NVL 的另一条并列产品线」。它首次亮相就在本代作为独立的 NVL144 系统，
   是三档域里的中间档；NVL576 则由 8 个**本代** MGX NVL 机架组成。两者是**不同机架世代**，
   不是同一硬件的两种叫法。
3. **命名口径改过**。2025-10 的 OCP 官方博客带编者按：「本文已更新，将品牌从
   Vera Rubin NVL144 改为 Vera Rubin NVL72」——同一批硬件换过名字。所以引用旧材料时
   先确认它用的是哪一版命名。

核对位置：`/report` 汇报页的代际叙事段 + 比较模式 `right=sys.rubin-ultra-nvl576` 的说明文字。

**环节 4.2 一页方案（60 分钟）**
通读 `/report` 汇报页，按它的**七节结构**写自己的一页方案，打印。

⚠️ **第 4 节「代际变化」v1.5 改过版式，读法跟着变**：它不再是一条把五个系统串起来的箭头链，
而是按**关系类型**分成三组，因为那五个系统里只有三个在同一条时间轴上：

| 分组 | 成员 | 怎么读 |
|---|---|---|
| 换代主线（读的是时间轴） | GB300 NVL72 → Vera Rubin NVL72 → Vera Rubin Ultra NVL576 | 同一条产品线的前后代 |
| 同代内的域选择（**不是换代**） | GB300 NVL72 ↔ HGX B300 | 同一颗 Blackwell Ultra、两种 scale-up 域（72 卡机架域 vs 8 卡服务器域），**两边都已量产** |
| 配对（**不是换代**） | Vera Rubin NVL72 ↔ Groq 3 LPX | 一起工作，不是谁接替谁 |

**这一条是售前会当场翻车的点**：HGX B300 用的是与 GB300 NVL72 **完全相同的 Blackwell Ultra
（B300）**，两者同代、现在都在量产，**比 Vera Rubin 早一代**。旧版汇报页按内容包声明顺序机械
串联，把它排在了 NVL576 与 LPX 之后，读起来像「NVIDIA 最新一代」——这是 v1.5 修掉的缺陷之一。
顺带：新版还渲染出了两条原先根本显示不出来的人工比较定义（`gb300-to-hgx-b300` 与
`gb300-to-rubin-ultra`），以及不再出现「没有叙述的裸 diff 表」。

**环节 4.3 汇报脚本与彩排（120 分钟）**
脚本（对着模拟器演示）：
2 min AI Factory 解决什么业务问题 → 4 min 硬件下钻（集群→板级）→
4 min 一次推理如何使用这些部件（数据流 + chips）→ 3 min GB300→Rubin 代际变化（比较模式）→
2 min 哪些是事实哪些是预测、下一步容量规划怎么做。
至少完整彩排 2 遍，第 2 遍录像。

**✅ 第 4 周验收线（= 终验）**
- [ ] 找同事当听众（或对录像自评）打 A 表，总分 ≥80/100
- [ ] 三个追问至少接住两个（A 表追问项 ≥6/10）
- [ ] 重做基线测试 ≥18/20
- [ ] 一页方案打印版完成

---

## 5b. NVIDIA 2026 更新 · Groq 3 LPX 与分离式推理（第 4 周后加课，60 分钟）

> 前四周讲的都是「一台 GPU 机架怎么把一次推理跑完」。2026 年 GTC 之后这句话不再完整了：
> NVIDIA 把 decode 拆成两半，交给两种**不同架构**的机器分头跑。这一节补上这条线。
> ⚠️ 先记住一条口径纪律：LPX 至今**没有官方规格表**，下面每个数字在工具里的证据徽章
> 都是「厂商宣称」（vendor_claim）而不是「官方规格」，对外引用要连同前提一起说。

**环节 5.1 LPU 是什么（20 分钟）**

▶ 打开 <https://caoruixin.github.io/aifactory-sim/?tour=scene.lpx.rack-anatomy>

- **预期看到**：一个机架里 32 个 1U 托盘整齐排开，比 Vera Rubin 的 18 个计算托盘密得多，
  而且**一个交换托盘都找不到**。
- **三问**（先自己答，再看左栏讲解卡）：
  1. 一个 LPX 机架里有多少颗加速器？它们叫什么？
  2. 单颗 LPU 的「显存」有多大——注意单位。
  3. 256 颗芯片之间靠什么连起来？和 Vera Rubin 的连法差在哪？
- **答案与核对**：256 颗 LP30（32 托盘 × 8）。单颗 **500 MB 片上 SRAM**——是 **MB 不是 GB**，
  对照 Rubin GPU 单卡 288 GB HBM4，容量差约 576 倍；换来的是 150 TB/s 片上带宽
  （Rubin 单卡 22 TB/s，约 6.8 倍）。**容量换带宽**就是 LPU 这条路线的全部交易内容。
  连法上 LPX **没有交换层**：每颗 LPU 96 条 112 Gb/s 的直连 C2C 链路，托盘内直连、
  跨托盘经 C2C spine，机架级 640 TB/s——对照 Vera Rubin 经 36 颗 NVLink 6 交换芯片的 260 TB/s。
- **一句话记忆**：*GPU 是「装得下、算得动」，LPU 是「吐得快、抖得少」。*
- 顺手注意界面细节：切到这一代之后，左栏平面开关里第一项不再叫「NVLink」，而是
  **「C2C scale-up（LPU 直连）」**——因为这一代真的没有 NVLink，写着 NVLink 就是讲错了。

**环节 5.2 AFD 三段流：一个 token 要跑两台机器（25 分钟）**

▶ 打开 <https://caoruixin.github.io/aifactory-sim/?tour=scene.lpx.afd-pairing>

- **预期看到**：画面里有**两个**机架——LPX，以及标着「AFD 对端」的 Vera Rubin NVL72，
  中间一条 scale-out 连线。
- **三段流**（这是本节唯一必须背下来的东西）：

  | 段 | 在哪台机器 | 干什么 | 为什么是它 |
  | --- | --- | --- | --- |
  | ① prefill | Vera Rubin NVL72（GPU） | 吃下长上下文，建好 KV cache | 吃并行算力与大内存 |
  | ② decode · attention | Vera Rubin NVL72（GPU） | 每出一个 token，在累积的 KV cache 上算全上下文注意力 | 吃 HBM 容量与带宽 |
  | ③ decode · FFN/MoE | Groq 3 LPX（LPU） | 跑稀疏专家前馈，结果回传 GPU | 小 batch 下要的是确定性低时延 |

  ②③ 每生成一个 token 就来回一趟，中间交换的是「中间激活」。官方把这个拆法叫
  **AFD（attention–FFN disaggregation，注意力—前馈分离）**，调度由 **NVIDIA Dynamo**
  用 KV-aware 路由完成。
- **三问**：
  1. 为什么不干脆全放 GPU 上跑？
  2. 官方宣称的 35× 是什么的 35 倍？前提是什么？
  3. 客户说「那我买 LPX 就行了吧」，怎么答？
- **答案与核对**：① 同一条流水线里 prefill 和 decode 的瓶颈完全不同——为峰值吞吐调优的硬件
  在最怕抖动的那一段并不划算，反之亦然；异构就是为了不做这个妥协。
  ② 是 **每兆瓦吞吐（TPS/MW）** 的 35 倍，而且有**三个必须同时说出口的前提**（v1.5 补齐第三条）：
  **① 对比 GB200 NVL72**；**② 400 TPS/用户** 这个交互度——低交互度场景（普通聊天）同构 GPU
  方案本来就够用，倍数不成立；**③ 万亿参数级模型**——产品页与 GTC 2026 发布稿都写着
  「for trillion-parameter models」，技术博客给出的那个 Pareto 样本点是**2 万亿参数 MoE +
  400K 输入上下文**。拿 35× 去讲一个 70B 模型就是超范围引用。
  **报这个数字必须带全三个前提**，否则就是营销复读。
  ③ 答「LPX 不单独卖，也不单独算」：它不做 prefill、片上只有 128 GB SRAM，
  官方所有性能口径都写着 paired with Vera Rubin。

**环节 5.3 为什么工具拒绝给 LPX 出产能（15 分钟）**

1. 切到 Groq 3 LPX 代际（顶栏第四个按钮，或
   ▶ <https://caoruixin.github.io/aifactory-sim/?gen=sys.groq3-lpx>），
   看顶栏右侧的提示条与产能卡。
2. 你会看到产能卡是**拒绝出数**的红卡，但它和 NVL576 那张红卡**理由不一样**：
   - NVL576 是 `analyst-modeled`：已官宣，但机架内部规格主要来自第三方分析师；
   - LPX 是 **`paired-only`**：官方从没给过「LPX 单独跑」的口径，
     所以这**不是缺一个数字**——注意它的「缺少的官方数据」列表是**空的**。
3. 去 `/report` 汇报页第 4 节末尾，读那张 LPX 专属拒绝卡与「配对而非换代」小节。
4. **自测**：客户拿来一份材料写着「Groq 3 LPX 单机架 N tokens/s」，你的第一句话是什么？
   —— *「这个数字的口径出处是哪里？官方只给过与 Vera Rubin 配对后的相对指标。」*

**环节 5.4 关系口径：别说成「NVIDIA 收购了 Groq」（5 分钟）**

2025-12 Groq 官方新闻室的原文只说了三件事：与 NVIDIA 签**非排他**推理技术**许可**协议；
创始人 Jonathan Ross、总裁 Sunny Madra 与部分团队**加入 NVIDIA**；
**Groq 继续作为独立公司运营**，GroqCloud 不中断。发布稿**没有提到任何金额**，
所以工具里也没有金额 Claim——坊间流传的数字没有官方出处，不要跟着报。

**✅ 本节验收线**
- [ ] 能在 60 秒内讲完 AFD 三段流，并说出每段为什么落在那台机器上
- [ ] 报「35×」时自动带出**三个**前提（对比 GB200 NVL72、400 TPS/用户、万亿参数级模型）
- [ ] 能说清 `paired-only` 与 `analyst-modeled` 两种拒绝出数的区别
- [ ] 不会把 NVIDIA×Groq 说成收购

---

## 5c. CPO vs NPO 光模块：证据分层练习（第 4 周后加课，20 分钟）

> Rubin Ultra NVL576 的导览新增了第 3 站，专门用来练一件事：**同一张讲解卡上，
> 哪句是分析师文章的事实陈述、哪句是本手册自己的解读**——这是全手册唯一一处
> 显式标了「⚠️ 这是本手册的解读」的段落，练的就是这种警觉，售前汇报里最容易被
> 懂行的客户当场拆穿的就是分不清这两层。

▶ 打开 <https://caoruixin.github.io/aifactory-sim/?tour=scene.ru.optics-formfactor>

- **预期看到**：切到板级、聚焦一个 NVSwitch 7 交换托架，16 个 NPO 光模块按 4×4 网格排布——
  4 颗 NVLink 7 交换芯片，每颗旁边 4 个插槽式模块。
- **三问**（先自己答，再看左栏讲解卡）：
  1. 眼前这台 3D 模型建的是 NPO 版还是 CPO 版？为什么不干脆两个都建？
  2. NPO 和 CPO 在「怎么换」这件事上差在哪？
  3. 左栏讲解卡第③段（「断了会怎样」）是 SemiAnalysis 原文写的，还是本手册自己推的？
- **答案与核对**：① **NPO 版**——CPO 是互斥的在研版本，同一棵装配树里两个都建等于建了
  一台不存在的机器，工具刻意只建其中一个（`asm.ru.optics` 恒为 16 个模块）；CPO 的关键
  量化事实（模块带宽、激光源具体数量）原文也没给，沿用「无来源部件不建实体」的先例。
  ② NPO 插槽式模块坏了换模块；CPO 光引擎与 ASIC 共封装、4 个不可更换，坏一颗大概率要连
  整颗 ASIC 一起处理——维保口径从「换模块」变成「换芯片」。
  ③ **是本手册自己推的**，卡片里用 ⚠️ 显式标了出来。原文只给了 NPO/CPO 的物理形态差异，
  没有讨论故障场景；「断了会怎样」是根据①②的结构差异推出来的合理外推，不是 SemiAnalysis
  的原话，更不是 NVIDIA 官方口径——对客户讲这段时要用「我们判断」而不是「原厂说」。
- **一句话记忆**：*同一张卡片上，事实、分析师判断、作者解读是三档不同的置信度，说的时候语气要跟着换。*

**✅ 本节验收线**
- [ ] 能一句话说清「为什么不把 CPO 也建成 3D 实体」
- [ ] 不看提示，能从讲解卡里指出哪一句是本手册自己的解读

---

## 5d. 国产超节点对照实战 · 判断超节点七问（deck 转述，45 分钟）

> 素材来源是一份 2026 WAIC 内部资料的转述（`src.waic2026-deck`，internal_deck 级别），
> **没有官方规格表佐证**——工具里凡是引用它的内容都只出现在讲解文案（narration /
> presalesNote / summary）里，不会有一条挂着这个来源的 `Claim`，`/report` 第 6 节
> 「国产超节点对照（WAIC 2026）」的每一段文字前面也都挂着「内部材料转述，非官方口径，
> 不构成规格」的徽标。这一节练的不是记数字，是**拿到几个孤立参数时，怎么判断自己还
> 缺什么**——这恰恰是售前最常被问住的场景。

**环节 5d.1 判断超节点好坏的七个问题（10 分钟）**

deck 原话是「判断超节点好坏，不能只看卡数和峰值算力」，给了七个必须依次追问的问题：

| # | 问题 | 追问的是什么 |
|---|---|---|
| ① | Scale-Up 域到底多大？ | 区分单柜卡数、紧耦合域、Scale-Out 集群总规模——三个不同的数，常被故意混着报 |
| ② | 交换 ASIC 和协议是什么？ | 是否自研、是否公开、GPU 端怎么集成 Endpoint IP |
| ③ | 有没有内存或事务语义？ | 统一寻址、远程显存、原子操作与集合通信卸载 |
| ④ | 真实带宽与尾时延如何？ | 不能只看聚合带宽，要看单卡带宽、P99 和拥塞 |
| ⑤ | 多卡扩展效率是多少？ | 8 卡到 64/128 卡，Token 吞吐是否接近线性增长 |
| ⑥ | 软件是否覆盖真实模型？ | MoE 路由、KV Cache、PD 分离、故障恢复与拓扑调度 |
| ⑦ | 是否已经稳定商用？ | 量产交付、长稳运行、客户案例和单位 Token 成本 |

第⑦问对应的「能跑→跑对→跑快→跑稳→跑省」商用成熟度框架，已经拆到第 3 周卡 A 那次
数据流讲解的「售前怎么解释」提示里了（ingress≈能跑、prefill≈跑对、decode≈跑快、
moe≈跑稳、egress≈跑省），这里不重复，只补一句 deck 的最终评价标准：
**不是有多少卡，而是每瓦、每元能稳定产出多少 Token。**

**环节 5d.2 练习：平台型三卡，哪一问答不上来（25 分钟）**

deck 把国产超节点厂商分成平台型（华为/阿里/百度昆仑芯）、AI 芯片、OEM-ICT、推理基础设施
四类；「平台型」这三家各自只给了 3~4 个孤立参数，**没有任何机柜内部结构数量**——这正是
练习的靶子。对着每张卡，把七问从①问到⑦，圈出「这几个参数答得上」的题号，再看下面的参考判断。

**卡 1 · 华为 Atlas 950 SuperPoD**
> 昇腾 950 NPU / 灵衢协议 / 1024 卡 / 256TB（统一内存编址）

参考判断：①能沾边答（1024 卡是规模量级，但没说清是紧耦合域还是含 Scale-Out 的总数）；
③能沾边答（「256TB 统一内存编址」暗示有统一寻址语义，但深到什么程度不知道）；
**②④⑤⑥⑦全部答不上来**——协议叫「灵衢」，但是否自研、是否对外公开、GPU 端怎么接入完全没说；
没有任何单卡带宽、尾时延、扩展效率、软件栈、商用案例的信息。

**卡 2 · 阿里磐久 AL128 × 真武 M890**
> 144GB 显存 / ICN Switch 1.0 / 单柜 128 卡 / 柜内 PB/s 级带宽

参考判断：①能答（单柜 128 卡，规模量级明确）；④只能沾边答（「柜内 PB/s 级」是聚合带宽的
数量级，不是单卡带宽也不是 P99，依然不完整）；②只有个名字「ICN Switch 1.0」，自研程度、
是否公开都不知道；**③⑤⑥⑦全部答不上来**。

**卡 3 · 百度昆仑芯 XPU**
> 单柜 32/64 卡，可扩展至 512 卡 / 协议未公开

参考判断：①能沾边答但边界模糊（32/64 卡是单柜、512 卡是扩展到什么域没说清，两个数混在一起）；
**②是这张卡里唯一被明说的答案——「协议未公开」**，这是七问里最干脆的「答不上来」案例：
不是「没提到」，是厂商自己说了「不公开」，连「是否自研」都判断不了；
**③④⑤⑥⑦全部没有信息**。

**共同结论**：三张卡都答不上④⑤⑥⑦（真实带宽与尾时延 / 多卡扩展效率 / 软件是否覆盖真实模型 /
是否已稳定商用）——这正是「拿到几个展台参数」和「能不能拿去做真实项目判断」之间的差距；
百度昆仑芯那张卡更进一步，连②都是被明确写成「未公开」，而不是「没提」。

客户拿着这类资料问你怎么看，标准答法不是「不知道」，而是：**「这几个参数能确认规模量级，
但七问里另外几问都没有官方口径可查——这正是方案里要明确写清楚、需要客户或厂商另外补的
信息缺口。」** 核对位置：`/report` 第 6 节「国产超节点对照（WAIC 2026）」的规模阶梯表与
结构差异三条（跨柜光互连 vs 铜背板、正交无背板 vs NVLink 背板、自研交换 ASIC vs NVSwitch）。

**环节 5d.3 规模阶梯换算（10 分钟）**

deck 给的行业口径是单柜 32–128 卡、Scale-Up 域 256–1,024 卡、集群万卡+。拿它对照本工具
四代：GB300/Vera Rubin 的 NVL72 = 72 卡，落在「单柜」档；Rubin Ultra 的 NVL576 = 576 卡
（8 柜跨柜域），落在「Scale-Up 域」档；Groq 3 LPX = 256 卡，虽然是单柜，但已经落进
「Scale-Up 域」档的下限——**单柜密度已经追上了行业说的跨柜规模量级**，这一点在跟客户对齐
「国产方案的单柜卡数为什么看起来比 NVL72 多」时很有用：不是本工具的 NVL72 落后，是这两条
产品线本来就分属不同的口径层级。

**✅ 本节验收线**
- [ ] 不看资料，能复述七问的关键词（域大小 / 交换 ASIC 协议 / 内存语义 / 带宽尾延迟 /
      扩展效率 / 软件覆盖 / 商用成熟度）
- [ ] 三张国产卡至少能各自指出 1 个「答不上来」的问题号，并说明为什么
- [ ] 能说清「协议未公开」（百度卡，厂商明说不公开）与「没有该项数据」（其余问题，deck 没提）
      的区别——这两种「答不上来」对售前的含义不一样
- [ ] 不会把 deck 转述的定性结构差异（跨柜光互连/正交无背板/自研交换 ASIC）当成可以对外
      引用的规格数字，也不会把三卡的孤立参数当成完整规格表

---

## 6. 第 6 周 · 领域切面

> v1.6 新增板块。前四周走的都是「整体→局部」的纵向下钻（机房→机架→托盘→板级）；
> 这一周换一个方向——**横着看**：同一件硬件，牵出的是哪个 serving runtime 技术
> （Dynamo NIXL / SHARP / GDS / KVBM / Model Streamer / PD 分离 / EP all-to-all），
> 又落到哪个推理环节、影响哪个业务指标（TTFT/TPOT/吞吐/冷启动/KV 命中率/扩展性/MTTR）。
> 中央仍是同一个 3D 场景，只是左右两栏换了一套叙事——**因果链**：硬件 → 技术 → 环节 · 指标。

**本周目标**：任选网络切面或存储切面里的一章，5 分钟内讲清它的完整因果链（谁连谁、
关键数字多少、支撑了哪个 serving 技术、影响哪个指标、断了会怎样），并能用两个切面
计算器给出量级估算（不是 SLA 承诺）。

**进入方式**：顶栏第三个模式按钮「切面」（续读上次章节，没有历史就进网络切面第 1 章）；
移动端在头部下方多一行同样的入口——未进切面时是「网络切面 / 存储切面」两个按钮，
进去之后同一排按钮兼任切面切换，右边多一个「退出」。下面每张任务卡的深链
（`?lens=network&chapter=N` / `?lens=storage&chapter=N`，`N` 从 1 起算）在桌面和
移动端都能直达，不必先点按钮。

**环节 6.1 网络切面：六张任务卡（90 分钟）**

每张卡对应模拟器里网络切面的一章：打开链接就落到该章 pin 的代际与层级，左栏是本
切面的章节列表，右栏是「narration（① 看到什么 ② 谁连谁+关键数字 ③ 没有这层会怎样）
→ 因果链 → 关键数字 → 代际对照」。**先自己答三问，再读右栏对答案**——因果链卡片里
硬件 chip 可以点开（选中该部件 + 弹出详情），技术 chip 点开是完整的技术注册表卡
（含出处链接）。

**卡 1 · NVLink 域：72 卡一跳互联的 KV 高速路**
- ▶ 打开：<https://caoruixin.github.io/aifactory-sim/?lens=network&chapter=1>
- 预期看到：整机架只亮 NVLink 平面——18 个计算托盘与 9 个交换托盘被绿色连线兜成一张网，
  没有一条线离开机架。
- 操作（3 步）：① 先答三问；② 展开第一条因果链卡（NVSwitch 交换托盘 + NVLink 铜背板 →
  `tech.ep-alltoall` → MoE 分发/合并 · TPOT/吞吐），点开技术 chip 看它的 summary/figures；
  ③ 展开第二条因果链（→ `tech.pd-disagg` → KV 写入 · TTFT），说清「域内一跳直达」
  和跨机「传文件」的区别。
- 自问三问：谁连谁？关键数字多少？断了会怎样？
- 核对位置：右栏「关键数字」每 GPU NVLink 带宽（**1.8 TB/s**）与 NVLink 域聚合带宽
  （**130 TB/s**）两行。⚠️ 都是**双向口径**——跟单向的网卡数字（如 800 Gb/s）并排比较前
  先统一口径（v1.5 订正纪律：「慢 18 倍」那笔错账就是这么算出来的）。

**卡 2 · 计算网：rail-optimized 双平面怎么喂跨机并行**
- ▶ 打开：<https://caoruixin.github.io/aifactory-sim/?lens=network&chapter=2>
- 预期看到：视角拉到机房总览，紫色线从每个机架穿出去找 Leaf 交换层，Leaf 再上联 Spine。
- 操作：① 答三问；② 展开 `tech.rail-routing` 与 `tech.adaptive-routing` 两条因果链，
  说清「1:1 GPU:NIC 按 rail 接线」和「逐包改道」分别解决什么；③ 回看卡 1，用一句话说清
  「计算网喂跨机并行、NVLink 域喂机架内并行」的分工。
- 自问三问：谁连谁？关键数字多少？断了会怎样？
- 核对位置：关键数字「每 GPU 计算网带宽」**800 Gb/s**（单向口径）、「双平面拆分」**2×400 Gb/s**、
  「Spectrum-X 对比现成以太网」**1.6 倍**（⚠️ 厂商宣称口径，对比对象是整套现成以太网方案，
  adaptive routing 单项收益官方未拆分出数）。

**卡 3 · 在网计算：SHARP 把 all-reduce 搬进交换芯片（跨代到 Vera Rubin）**
- ▶ 打开：<https://caoruixin.github.io/aifactory-sim/?lens=network&chapter=3>
- 预期看到：顶栏代际徽章自动从 GB300 跳到 **Vera Rubin NVL72**——机架骨架没变
  （还是 18 计算托盘 + 9 交换托盘），但交换托盘换成了 4 颗 NVLink 6 芯片（GB300 是 2 颗
  NVLink 5）。相机只飞一次，不会看到「先落总览再跳焦点」的两段闪跳。
- 操作：① 答三问；② 展开 `tech.sharp` 因果链，说清「加法搬进交换芯片」省的是 TP
  并行每一层的哪一轮通信；③ 想一想这一章为什么会自动换代际——章节把 `systemId` 焊死了，
  这是切面「跨代 pin」的第一个实例。
- 自问三问：谁连谁？关键数字多少？断了会怎样？
- 核对位置：关键数字「SHARP 在网规约算力」**14.4 TFLOPS FP8**（每交换托盘，NVLink 6 代）；
  同一行下面「GB300 代 NVLink 交换的 SHARP 算力」故意留 **null**——官方没公布上一代的数字，
  不能从 14.4 倒推，这正是「查不到 ≠ 官方说没有」纪律的活教材。

**卡 4 · 业务存储网：KV 卸载与模型拉取的物理通道**
- ▶ 打开：<https://caoruixin.github.io/aifactory-sim/?lens=network&chapter=4>
- 预期看到：机房总览只亮蓝色业务网——每个托盘的 BlueField-3 接到汇聚交换层，
  再连到 L2 共享存储与更外圈的 L3 对象存储，路径与计算网完全不重叠。
- 操作：① 答三问；② 依次展开三条因果链（`tech.kvbm` KV 卸载 / `tech.nixl` 统一搬运接口 /
  `tech.model-streamer` 模型权重拉取），说清三者各管哪一段；③ 读「KV 卸载到网络存储」那行
  的官方原句，记住它——存储切面第 3 章会原句再出现一次（同源同句，content 测试有一致锁）。
- 自问三问：谁连谁？关键数字多少？断了会怎样？
- 核对位置：关键数字「存储带宽上限（每计算节点）」**40 GB/s**、「BlueField-3 接入带宽」
  **双 400 Gb/s**、「KV 卸载到网络存储」官方原句——「分布式推理把 KV cache 卸载到高速网络
  存储」是 HGX 参考架构点名的未来负载。

**卡 5 · 管理网：带宽最小的网守着 MTTR**
- ▶ 打开：<https://caoruixin.github.io/aifactory-sim/?lens=network&chapter=5>
- 预期看到：机房总览只剩灰色细线——机架内管理交换机把托盘 BMC、电源架、DPU 全部收拢，
  再上联到机架外的带外管理汇聚，六张网里带宽最小、存在感最低的一张。
- 操作：① 答三问；② 这条因果链的技术层写着「硬件直达指标、中间没有 serving 技术」——
  对照前四张卡，说清为什么这一章没有技术 chip；③（可选，移动端）用头部的代际 `<select>`
  手动切一个代际，观察左栏切面区如何显式提示「本章视角已失效」而不是被悄悄打回导览——
  这正是 W-D 补的守卫要挡住的坑。
- 自问三问：谁连谁？关键数字多少？断了会怎样？
- 核对位置：关键数字 SN2201 管理交换机 **48 口**、BMC 带外口 **1 Gb/s**；MTTR 参考值
  故意留 **null**——官方不给 MTTR 目标，这笔账要拿客户自己的运维基线来算。

**卡 6 · 域的大小决定并行方式（HGX 对照）+ KV 交接计算器（跨代到 HGX B300）**
- ▶ 打开：<https://caoruixin.github.io/aifactory-sim/?lens=network&chapter=6>
- 预期看到：代际切到 **HGX B300**，同样开着 NVLink 平面，机架里却**一条绿线都没有**——
  这一代的 NVLink 域装在服务器里面（板载 NVSwitch），出了 8 卡就没有域了。
- 操作：① 答三问；② 展开「动手算一算」计算器（`data-lens-calc="kv-transfer"`），输入一个
  KV 体积，对比域内 NVLink（1.8 TB/s 双向）/ 跨域 CX-8（800 Gb/s 单向）/ 存储网（40 GB/s）
  三档耗时；③ 点右栏底部「代际对照」链接回第 1 章 GB300 NVLink 域，说出两台机器在
  「并行方式需要多大的域」这道选型题上的取舍。
- 自问三问：谁连谁？关键数字多少？断了会怎样？
- 核对位置：关键数字「板内 NVLink 聚合带宽」**14.4 TB/s**（双向，8 卡基板）、「跨机计算网
  带宽」**800 Gb/s**（单向端口速率）——⚠️ 两个数字口径不同，并排放是为了看量级差，
  不是拿来相除算倍数的（v1.5 订正纪律的代码化）。计算器给的是理论下限，讲量级、别报 SLA。

**环节 6.2 存储切面：五张任务卡（75 分钟）**

存储切面沿推理业务动线走——**分发 → 加载 → 运行时 KV → 归档 → RAG**，章节顺序本身
就是业务顺序（不是按容量/带宽这类硬件维度排的）。

**卡 7 · 模型分发：L3 对象存储是货仓**
- ▶ 打开：<https://caoruixin.github.io/aifactory-sim/?lens=storage&chapter=1>
- 预期看到：机房总览业务网最外圈多了一个盒子——L3 对象存储；从它出发经汇聚交换层、
  L2 共享存储，再到托盘里的 BlueField-3，整条分发路径被点亮。
- 操作：① 答三问；② 展开 `tech.model-streamer` 因果链，说清「先落盘再转格式」和
  「S3 直读」两条路径的差别；③ 读左栏对象存储那行的 ⚠️ 提示——这个盒子是本项目对
  行业通行架构的建模示意，参考架构不涉及对象存储选型，容量/吞吐都不能报数。
- 自问三问：谁连谁？关键数字多少？断了会怎样？
- 核对位置：关键数字「L3 对象存储聚合吞吐」故意留 **null**（官方未覆盖这一层）；
  「S3 直读加载耗时」**Model Streamer 4.88 秒 vs Tensorizer 37.36 秒**（官方基准，
  15 GB Llama-3-8B，concurrency 32 对 16 workers；Safetensors 原生 loader 不支持 S3，
  没进这一局）。

**卡 8 · 冷启动：把权重塞进 HBM 要几秒 + 加载计算器**
- ▶ 打开：<https://caoruixin.github.io/aifactory-sim/?lens=storage&chapter=2>
- 预期看到：视角下到机架，业务平面亮着——托盘里的 E1.S 缓存盘与 BlueField-3 被点亮，
  蓝色线通向机架外的共享存储。
- 操作：① 答三问；② 展开 `tech.gds` 因果链，说清 GDS 的 DMA 直达路径省了哪一段
  （CPU 内存不再是中转站）；③ 打开「动手算一算」计算器（`data-lens-calc="model-load"`），
  输入一个模型体积，看各段瓶颈与串行总时长。
- 自问三问：谁连谁？关键数字多少？断了会怎样？
- 核对位置：关键数字「存储带宽上限（每计算节点）」**40 GB/s**；因果链叙述里同一块本地盘
  「并发流式读 14.34 秒 vs 单线程顺序读 47.99 秒」；「E1.S 缓存盘带宽」故意留 **null**——
  参考架构只给了 E1.S 的数量与容量，且两处数量写法不一致（已在数据里留痕），没有任何
  带宽数字。
- ⚠️ 别把两组数字搞混：4.88s/37.36s 是**跨网络的 S3 加载**（卡 7 同一组数字），
  14.34s/47.99s 是**同一块本地盘上**并发读 vs 单线程读的差距——瓶颈不同，别混着引用。

**卡 9 · 运行时 KV：显存不够、层级来凑 + 恢复计算器（跨代到 HGX B300）**
- ▶ 打开：<https://caoruixin.github.io/aifactory-sim/?lens=storage&chapter=3>
- 预期看到：代际切到 **HGX B300** 的机架——服务器里的本地 NVMe 数据盘与 BlueField-3
  亮起，蓝线通向机架外的共享存储。这一代是「KV 分层」被官方点名的一代。
- 操作：① 答三问；② 展开 `tech.kvbm`（KV 当分层内存的块管）与 `tech.nixl`（跨节点/跨层
  搬运）两条因果链；③ 打开恢复计算器（`data-lens-calc="kv-restore"`），对比「从层级捞回
  KV」和「重算 prefill」哪边更便宜。
- 自问三问：谁连谁？关键数字多少？断了会怎样？
- 核对位置：「KV 卸载到网络存储」官方原句（与网络切面卡 4 同源同句）；「Mooncake 有效请求
  容量提升」**+59%~498%**（FAST 25 真实 trace 回放）；「生产集群请求量提升」**A800 +115% /
  H800 +107%**（Kimi 实际部署）；「KV cache 命中率」故意留 **null**——命中率由业务前缀
  共享度决定，没有可脱离负载引用的通用数字。⚠️ 只认 FAST 25 正式版数字，arXiv 版写的是
  525%/75%，版本不同不要混用。

**卡 10 · 归档与镜像：MTTR 的另一半**
- ▶ 打开：<https://caoruixin.github.io/aifactory-sim/?lens=storage&chapter=4>
- 预期看到：回到 GB300 机房总览，只有最外圈的 L3 对象存储和它到汇聚层的那条线亮着。
- 操作：① 答三问；② 对照网络切面卡 5（管理网），用一句话说清「管理网管多快发现坏了，
  归档层管坏了之后多快回来」；③ 数一数这一章关键数字栏有几行是 null——这本身就是
  要传达的信息（RTO 取决于客户侧的归档吞吐 × 副本布局 × 重放流程，三个变量都不在
  参考架构范围内）。
- 自问三问：谁连谁？关键数字多少？断了会怎样？
- 核对位置：关键数字「归档/恢复吞吐（RTO 的分母）」——**全表唯一一行，且是 null**。

**卡 11 · RAG 与 L4：数据库/向量库（纯叙事，3D 刻意不动）**
- ▶ 打开：<https://caoruixin.github.io/aifactory-sim/?lens=storage&chapter=5>
- 预期看到：3D 场景**不会跳转、不会高亮任何部件**——数据库和向量库不在机架里，
  这本身就是本章的教学点：参考架构完全不覆盖这一层。
- 操作：① 打开链接前先猜一猜「3D 为什么不动」，再对答案；② 展开唯一一条因果链，
  注意它的硬件 chip 位置写着「不经硬件 · 这一层不在机架里」（`hardwareRoleKeys` 是空
  数组）；③ 说清 WEKA/VAST 两组数字为什么不能直接抄给客户当规格。
- 自问三问（本章没有硬件，三问换一个角度）：检索发生在推理流程哪一步（提示：TTFT 之前
  还是之后）？关键数字是谁说的？没有这层会怎样？
- 核对位置：关键数字「WEKA：TTFT 改善倍数」**41 倍**（128K 上下文，厂商自测）、
  「VAST：重算 vs 从存储加载 KV」**62 秒 → 3 秒**（Llama 3.1-405B、127,188 token、H100，
  厂商自测）——两行证据徽章都是**厂商宣称**，听个方向、别当规格报给客户。

**环节 6.3 关键数字过关 + 计算器实操（45 分钟）**

默写下面这组数字（不看单位不算过），再逐个到对应章节核对：
1.8 TB/s（NVLink，双向）· 130 TB/s（域聚合，双向）· 800 Gb/s（计算网，单向）·
40 GB/s（存储，每节点）· 14.4 TFLOPS（SHARP，NVLink 6 代）· 14.4 TB/s（HGX 板内，双向）·
4.88s / 37.36s（S3 加载）· 41 倍（WEKA，厂商宣称）。

两个计算器各跑一遍，读一遍它们各自的 headline caveat 与「未建模」清单（协议开销/并发
争用/条带化/排队），确认自己能对客户说清「这是理论下限，不是 SLA 承诺」。

**环节 6.4 脱稿讲解（45 分钟）**
从网络切面挑 1 章、存储切面挑 1 章（建议卡 1 + 卡 9，一个纯硬件一个带计算器），各 3 分钟
讲完整因果链：硬件 → 技术 → 环节 → 指标 → 断了会怎样，录音自评。

**✅ 第 6 周验收线**
- [ ] 11 张切面任务卡走完，抽 4 张（网络 2 + 存储 2）能不看提示复述完整因果链
- [ ] 环节 6.3 的 8 组数字闭卷默写 ≥6/8 全对（含单位与双向/单向口径）
- [ ] 两个计算器各跑通一次，能说出各自的「理论下限」措辞与至少 2 项「未建模」
- [ ] 60 秒内说清两条切面的组织逻辑差异：网络切面按六张网各自的角色排，存储切面按
      「分发→加载→运行时→归档→RAG」的业务动线排
- [ ] 自检题：客户问「这些切面章节和左边的下钻导览是什么关系」——能答出「同一个 3D
      场景，导览是纵向看物理结构，切面是横向看因果链，两条路径不互斥」

---

## 7. 术语卡系统（30 张）

**卡片模板**（正面 3 行，背面 1 行）：

```
正面：术语名
  ① 主线位置：业务负载→…→POC 的哪一段
  ② 一句话作用
  ③ 影响的指标（tokens/s / TTFT / TPOT / tokens/W / TCO / 可靠性）
背面：模拟器核对位置（部件 id 或深链）+ 证据等级
```

**复习协议（可衡量）**：每天 10 分钟抽 10 张，每张 ≤20 秒说出三要素才算过；
把当日通过率记进追踪表。周末全量抽检，**≥27/30 为过关**；连续两天 100% 的卡移出每日池、只进周末池。

## 8. 评分表

**A 表 · 15 分钟终验汇报（100 分）**

| 维度 | 分值 | 评分要点 |
|---|---|---|
| 结构与时间 | 20 | 六段齐全、总时长 13~17 分钟、过渡自然 |
| 技术准确 | 25 | 关键数字准确、无编造；说错一处 −5 |
| 证据边界 | 15 | 主动区分官方规格/厂商宣称/分析师预测，NVL576 明确标预测 |
| 演示操作 | 15 | 下钻、数据流、比较模式操作流畅，不迷路不冷场 |
| 表达 | 15 | 脱稿；卡壳每次 −2，读稿一次 −5 |
| 追问 | 10 | 3 个追问，每个答到「主线位置 + 影响指标」记 3~4 分，答不上记 0 |

**追问题库（听众从中任选 3 个）**：为什么不能只看卡数和 FLOPS？/ 为什么必须液冷？/
NVLink 断一条会怎样？/ KV Cache 会把显存吃爆吗？/ MoE 对网络提了什么新要求？/
这套产能数字敢写进合同吗，为什么？

**B 表 · 5 分钟讲解自评（16 分，每维 0~4）**：链路顺序完整 / 数字准确 / 术语挂上主线 / 流畅度。

## 9. 进度追踪表

| 里程碑 | 日期 | 得分 | 备注 |
|---|---|---|---|
| 基线测试（学前） | | /20 | |
| 第 1 周验收 | | 4 项中过 项 | |
| 第 2 周验收 | | 5 项中过 项 | |
| 第 3 周验收 | | 5 项中过 项 | |
| 终验汇报（A 表） | | /100 | 追问 /10 |
| 基线重测 | | /20 | 提升 = 重测 − 初测 |

| 术语卡每日抽检 | 通过率 |
|---|---|
| （日期） | /10 |

---

## 附录 A · 自测答案与核对位置

> 先闭卷作答再看。每题给「答案要点 + 在模拟器里的核对位置」；核对不过的当场点开确认。

1. **18 个计算托盘、9 个 NVLink 交换托盘**。核对：`?level=rack&focus=asm.gb300.rack` 右栏。
2. **2× Grace CPU + 4× B300 GPU + 4× ConnectX-8 + 1× BlueField-3 + 本地 NVMe，全液冷**。核对：托盘详情。
3. **72 = 18 托盘 × 4 GPU**；NVL72 指一个 NVLink 域内 72 颗 GPU。核对：机架详情。
4. **Scale-Out 叶/主干交换、CDU、外置存储、管理节点、机房配电柜**。核对：`?level=cluster` 全平面。
5. **客户买的是持续产出的 token 吞吐与延迟承诺，不是硬件本身**。核对：`/report` 第一节。
6. **NVLink / Scale-Out / 业务与存储 / 管理 / 供电 / 液冷**。核对：左栏平面开关。
7. **域大小、带宽、延迟量级不同**：机架内 scale-up 一跳全连做张量/专家并行，跨机架 scale-out
   做数据/流水并行与东西向流量；混成一张网两头都做不好。核对：两平面对比。
8. **1.8 TB/s**（GB300 / 第五代 NVLink，每 GPU）。注意代际口径：**Vera Rubin 一代是 3.6 TB/s**
   （第六代 NVLink，翻倍）。核对：`asm.gb300.b300-gpu` 详情；代际对照见
   `?tour=scene.rubin.learn-gen-delta`。
   · ⚠️ **这个数字是双向合计**（v1.5 补）：参考架构原文写得很清楚——「a bandwidth of
     **900GB/s (1800 GB/s bi-directional)**」，技术博客也写「1.8 TB/s **bidirectional**
     (18 links × 100 GB/s)」。**报倍数前先对齐方向口径**：拿双向的 1.8 TB/s 去除单向的
     800 Gb/s 网卡会得出「慢 18 倍」，那是错的；同口径下是 **9 倍**——HGX 平台页规格表相邻
     两行给了同口径的独立佐证（整机 NVLink 14.4 TB/s vs 网络 1.6 TB/s，14.4 ÷ 1.6 = 9）。
     这是 v1.5 修掉的错误里传播最广的一条，因为它正好是最顺口的那句售前话术。
9. **市电 → 机房配电（列头柜/母线）→ 电源架 → 直流母排 → 托盘**。核对：`?level=cluster&planes=power` 顺线走。
10. **CDU 在设施水（一次侧）与机架冷板回路（二次侧）之间做热交换与流量控制**。核对：CDU 详情 + cooling 平面。
11. **三步：第 1 步（网关鉴权与调度）、第 6 步（MoE Router——选专家是计算内的逻辑决策，真正的物理搬运在第 7/8 步）、第 10 步（计费与日志）**。核对：步骤卡「逻辑层/物理层」徽章。
12. **Prefill 吃算力（算力墙），Decode 吃显存带宽（带宽墙）**。
13. **KV Cache 在 HBM 里**；chips = B300 GPU、HBM3e 显存堆栈、Grace CPU。核对：第 4 步 chips。
14. **每个 token 要分发到不同专家再汇总，All-to-All 通信量随规模上升，走 NVLink 域**。核对：第 6~8 步。
15. **实际产能被算力墙/带宽墙（roofline）与 MFU/MBU 区间约束**，峰值 FLOPS 只是上界的上界。核对：产能粗估 caveats。
16. **TTFT 由 Prefill 决定，TPOT 由 Decode 决定**。核对：产能粗估改上下文长度观察两者。
17. **带宽翻倍、网卡换代且翻倍、交换芯片翻倍**——三个「翻倍」：
    ① scale-up 每卡 NVLink **1.8 → 3.6 TB/s**（机架级 130 → 260 TB/s）；
    ② 网卡 **ConnectX-8 × 4/托盘 → ConnectX-9 × 8/托盘**（每张 GPU 独占 2 张 800 Gb/s 单口卡）；
    ③ 交换芯片 **每交换托盘 2 → 4 颗**，托盘数不变仍是 9 个，于是机架内 **18 → 36 颗**。
    机架结构本身没变（还是 18 计算托盘 + 9 交换托盘），但功率与配电要重算，
    而**官方至今未公布 Vera Rubin 整机架功率**。
    核对：`?tour=scene.rubin.learn-gen-delta` 与比较模式 diff 列表。
18. **三大类：官方规格（verified_spec）/ 厂商宣称（vendor_claim）/ 分析师预测（analyst_estimate、forecast）。**
    工具内部其实分 **7 档证据等级**（另有 benchmark 公开跑分、management_guidance 业绩会指引、
    author_opinion 本项目推导），来源类型则分 6 类，**v1.3 新增「媒体报道」（media_report）**
    ——媒体数字只能配 forecast 用，转述的厂商回应只能出现在备注里。
    售前对外**记三大类就够**，但要知道第四种存在：凡是标 author_opinion 的（例如产能粗估）
    不是任何一方的承诺。核对：`/report` 第五节「证据等级」表 + 任意规格行的徽章。
19. **官方未公布 Vera Rubin 整机架功率，分母缺失即拒绝出数**——这是特性不是缺陷。
    注意它与第 20 题不是同一条规则：Vera Rubin 走的是常规策略（`standard`，缺哪一项就那一项不出数），
    NVL576 走的是整体拒绝策略。核对：切代后产能 tab。
20. **NVL576 已经官宣，但工具仍然整体拒绝出产能——这两件事不矛盾。**
    · **官方已确认的**：GTC 2025-03 官宣，2026-03 的 POD 博客给出三档 NVLink 域
      （NVL72 / NVL144「Kyber」/ 旗舰 NVL576），官方全名 **NVIDIA Vera Rubin Ultra NVL576**，
      拓扑是 **8 个独立 MGX NVL 机架 × 72 张 GPU = 单一 576-GPU NVLink 域**（原型代号 Polyphe），
      **拓扑类别官方也点了名**：POD 博客原文「introduces a new **two-layer all-to-all** NVLink
      topology」（两层全互连）。因此这一代在工具里的状态是 **announced（已发布）而不是 forecast**。
    · **官方没给的**：机架内部具体规格（托架分层、交换托架高度、连接器与光模块细节）
      **主要来自第三方分析师**；整机架功率无人公布；
      2026 年的官方材料**不再给日期**（2025-03 GTC 现场说过「2027 下半年」）。
    · ⚠️ **拓扑名分两层说**（v1.5 订正——旧版手册把这一条归错了边）：官方词是
      **two-layer all-to-all**，分析师（SemiAnalysis）的归类才是「**Dragonfly**」。
      两者**不等价**，不能互相替换、更不能拿 Dragonfly 去解释官方那句。讲的时候先说官方词、
      再说分析师词，并注明后者是谁说的。
    · **所以**：工具给这一代的产能策略是 `analyst-modeled`——「已官宣，但规格主要来自第三方
      分析师（forecast 数据），不出产能」。有人报 NVL576 的 PFLOPS 或 token 产能数字，先问出处。
    · 延期传闻要说准：2026-07 媒体报道里写明年份「2028」的是**另一个产品 Kyber NVL144**，
      对 NVL576 只说「可能延期或仅限小批量」、没给年份；NVIDIA 的「路线图没问题」是
      **媒体转述**的回应，不是独立声明。
    核对：`?gen=sys.rubin-ultra-nvl576` 的顶部警示条与产能卡拒绝文案 + 该系统的「售前怎么解释」。

**5c 加课自测（不计入基线测试 20 题）**

21. **CPO vs NPO 讲解站里，哪句是分析师说的、哪句是本手册自己解读的？**
    · **分析师原文**（SemiAnalysis，p.9–p.10）：NPO 插槽式模块 vs CPO 内嵌 4 个不可更换光引擎
      + 外置激光源模块，以及「NPO 封装形态更成熟、会先于 CPO 投入市场」的判断。
    · **本手册解读**（讲解卡第③段，⚠️ 标注）：「断了会怎样——NPO 换模块、CPO 换芯片」
      这句话是根据上面的结构差异推出来的合理外推，原文没有讨论故障场景，也不是 NVIDIA
      官方口径，对客户讲时要用「我们判断」而不是「原厂说」。
    核对：`?tour=scene.ru.optics-formfactor` 左栏讲解卡第③段。

## 附录 B · 深链速查

| 参数 | 例子 | 作用 |
|---|---|---|
| `tour` | `?tour=scene.gb300.learn-plane-nvlink` | **直达某个导览站**：层级/焦点/平面/讲解文案/3D 高亮一次到位（本手册练习卡用的就是它） |
| `level` + `focus` | `?level=rack&focus=asm.gb300.rack` | 直达某层级/某部件 |
| `planes` | `?planes=power,cooling` | 只开指定平面（逗号分隔） |
| `gen` | `?gen=sys.rubin-ultra-nvl576`、`?gen=sys.hgx-b300` | 切换代际（五代：`sys.gb300-nvl72` / `sys.vera-rubin-nvl72` / `sys.rubin-ultra-nvl576` / `sys.groq3-lpx` / `sys.hgx-b300`，v1.4 起） |
| `mode` + `right` | `?mode=compare&right=sys.vera-rubin-nvl72` | 比较模式。`right` 会被清洗：未知代际或与左侧同代一律回落到一个合法的他系统 |
| `lens` + `chapter` | `?lens=network&chapter=1` | **直达某条切面的某一章**（v1.6 起）：`lens` 接受短名（`network`/`storage`）或全 id（`lens.network`）；`chapter` 从 1 起算，越界回落第 1 章 |
| `motion=off` | `?motion=off` | 关动画逐步读文案 |
| `gl=off` | `?gl=off` | 2D 降级（低配设备/投屏兜底） |

**CPO vs NPO 练习站深链**（5c 节用的就是它）：`?tour=scene.ru.optics-formfactor`——
直达 Rubin Ultra NVL576 第 3 站，板级聚焦交换托架，NPO/CPO 证据分层讲解一次到位。

**切面章节深链速查**（v1.6 新增，第 6 周 11 张任务卡用的就是这 11 条；桌面/移动端通用）：

| 切面 · 章节 | 深链 |
|---|---|
| 网络 1 · NVLink 域：72 卡一跳互联的 KV 高速路 | `?lens=network&chapter=1` |
| 网络 2 · 计算网：rail-optimized 双平面怎么喂跨机并行 | `?lens=network&chapter=2` |
| 网络 3 · 在网计算：SHARP 把 all-reduce 搬进交换芯片（跨代 Vera Rubin） | `?lens=network&chapter=3` |
| 网络 4 · 业务存储网：KV 卸载与模型拉取的物理通道 | `?lens=network&chapter=4` |
| 网络 5 · 管理网：带宽最小的网守着 MTTR | `?lens=network&chapter=5` |
| 网络 6 · 域的大小决定并行方式（HGX 对照）+ KV 交接计算器（跨代 HGX B300） | `?lens=network&chapter=6` |
| 存储 1 · 模型分发：L3 对象存储是货仓 | `?lens=storage&chapter=1` |
| 存储 2 · 冷启动：把权重塞进 HBM 要几秒 + 加载计算器 | `?lens=storage&chapter=2` |
| 存储 3 · 运行时 KV：显存不够、层级来凑 + 恢复计算器（跨代 HGX B300） | `?lens=storage&chapter=3` |
| 存储 4 · 归档与镜像：MTTR 的另一半 | `?lens=storage&chapter=4` |
| 存储 5 · RAG 与 L4：数据库/向量库（纯叙事，3D 刻意不动） | `?lens=storage&chapter=5` |

**参数一起用时的规矩**：`tour` 是基座，其余参数在它之上逐项覆盖——
`planes` / `level` / `focus` 会盖掉场景自带的那一项，显式 `mode` 会盖掉 `tour` 强制的导览态
（例如 `?tour=…&mode=explore` = 落到那一屏但不进导览、不出场景高亮，做对照实验很好用）。
两条边界：`focus` 必须属于最终生效的那个代际，跨代际会被忽略；
`tour` 与 `gen` 指向**不同代际**时场景序号不复用——加 `mode=tour` 会落到新代际的第 1 站，
不加则退出导览。
`lens` 与 `tour` 是同一层级的两个基座，两者都出现时 **`lens` 赢**（进切面，不进导览）；
`lens`+`chapter` 与 `gen` 同时出现且指向不同代际时，以**章节 pin 的代际**为准。

## v1.5 订正记录（已上线，本手册相应段落已同步）

> v1.5 不加系统、不加功能，做的是一件事：**以真实用户身份走查五个代际，把每条 Claim 拿去
> 逐条实访官方原文核对**。结果是 40 条数据订正 + 4 个界面缺陷。列在这里不是为了记流水账，
> 而是因为**这些错法本身就是教材**——它们全都不是「查不到资料」，而是「资料看得不够仔细」。

**四类最值得记住的错法**

1. **口径混用**（最危险，因为算术是对的）。「慢 18 倍」= 双向 NVLink ÷ 单向网卡；
   「BF-3 480 Gb/s」= 拿节点南北向汇聚网带宽当芯片规格；「单卡 C2C 1.8 TB/s」= 把每
   **超级芯片**的数字挂到单卡上（× 72 会推出 129.6 TB/s，官方整机架只有 65 TB/s）。
   → **每个倍数、每个比值，先问「两边是同一个口径吗」。**
2. **官方原句被截半**。Vera Rubin 的 cable-free 修饰的是**托盘**，机架后部是 4 个铜缆匣、
   约 5,000 根铜缆；旧版把它讲成「整机架无线缆」，在客户现场会被当场推翻。
   35× 漏掉「万亿参数模型」这个前提，同理。
   → **引用官方句子时把限定成分一起抄下来，省略号最容易吃掉的就是限定语。**
3. **把「我没查到」写成「官方没公布」**。每机架交换机台数、托盘 BMC 口速率、
   Overview 首句的 2-4-5-800 口径，官方其实都写了。NVL576 的拓扑类别官方也点了名
   （two-layer all-to-all），旧版却说「官方没点名任何拓扑」并把分析师的 Dragonfly 当官方讲。
   → **说「官方未公布」之前，先确认自己读完了那份文档的每一页。**
4. **官方自己前后矛盾时只选一边、不留痕**。SN5600 vs SN5610、NCCL 主机侧 vs ConnectX-8
   硬件侧做双平面负载均衡、appendix 与正文的交换机台数——都是同一份文档内部打架。
   → **两说并存 + 写明取哪个、为什么**，比悄悄选一个更经得起追问。

**界面侧修掉的 4 个缺陷**（都会直接影响你讲解时看到的东西）

- 汇报页第 4 节不再把 HGX B300 排成「最新一代」，改按关系分三组（见 4.2 节的表）
- 讲解文案里的 `**粗体**` 不再显示成字面星号
- 规格表显示中文标签（「整机架功率」而不是 `rackPowerKW`），原字段名移到悬停提示
- 「同一组件还出现在」不再给出跨代际的同名链接（点了会让右栏和顶栏对不上）

**顺带一条工程教训**：截图基线有 2% 像素容差，v1.5 里有 4 张基线**内容确实变了却照样通过**
（汇报页那张的改动只占 0.4% 像素）。靠基线守内容是有盲区的，最后是靠全量强制重拍 + 人工看图
兜住的。

---

## 之后（工具的二期方向，学有余力再看）

- WorkloadProfile/SLA 与 goodput 建模（「token 数量 ≠ 业务价值」）
- ~~HGX 参考方案~~ → **v1.4 已交付**：HGX B300 第五系统（NVLink 服务器域，顶栏第五个按钮）
- TPU7x 内容包 + 3D Torus 引擎改造（v1.4 已规划为 W-D/E，实施前单独评审）
- 国产超节点完整 3D 包——v1.4 已先行交付 `/report` 对照段与 5d 章七问练习；
  等华为 Atlas 950 / 阿里磐久 AL128 官方规格（可访问 URL）再升级建包，设计存档见 PLAN-v1.4.md W-B
- 正式 BOM / TCO / 可承诺产能（首版刻意不做，避免伪精确）
- HGX 的 keySpecs 进 `/report` 规格表（KeySpecTable 现硬编码 GB300）、HGX 推理数据流剧本
  （FlowEpisode 现仅 GB300 有完整剧本）
